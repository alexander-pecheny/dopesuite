{
  "name": "dope-web",
  "private": true,
  "description": "Frontend toolchain for the dope game pages (ADR-0003): esbuild bundles the TypeScript shell + protocol renderers; tsc typechecks.",
  "scripts": {
    "build": "node scripts/webbuild.mjs",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0"
  }
}
