# Текущая реализация

Сейчас приложение обслуживает один бой Эрудит-квартета.

## Backend

- Go-сервер в [main.go](/Users/pecheny/dope/main.go) хранит один `MatchState`.
- Состояние читается и пишется в `match_state.json`; если файла нет, создается `defaultMatch()`.
- Роуты: `/host`, `/viewer`, `/api/state`, `/api/update`, `/events`.
- `/events` отдает Server-Sent Events с полной проекцией состояния после каждого изменения.
- `Finished=true` блокирует правки боя, кроме отдельного действия переоткрытия.

## Модель боя

- У боя есть `title`, `finished`, `revision`, `updatedAt`, список команд.
- У команды есть `name`, строковый `roster`, 12 обычных тем, темы перестрелки, ручное место.
- У темы есть выбранный игрок и 5 отметок ответов.
- Значения вопросов: `10, 20, 30, 40, 50`.
- `right` добавляет очки, `wrong` вычитает, пусто не влияет.
- `Σ+` считает только положительные ответы обычного боя.
- Перестрелка считается отдельно в `shootoutTotal/tiebreak`.

## Frontend

- `static/host.js` строит редактируемую таблицу: ответы, игроки, места, финиш боя, добавление/удаление перестрелки.
- `static/viewer.js` строит read-only таблицу зрителя.
- `static/styles.css` уже содержит основной табличный язык: sticky-колонки, компактные ячейки, состояния ответов, шапку host/viewer.

## Покрытие

Тесты проверяют счета, перестрелку, ручные места, запрет правок законченного боя и нормализацию отметок.

## Ограничения

Нет SQLite, ID команд/игроков, нескольких боев, площадок, сетки чемпионата, пересевов и общей статистики по турниру.

## Положение после v2

После v2 БД заводится пустой: ни турниров, ни игр, ни матчей. Турниры создаются явно — в проде через будущий UI авторизованного пользователя, в тестах — через хелпер `bootstrapDefaultTournament(db, defaultMatch())`, который ставит системного пользователя владельцем и создает одну игру с дефолтным бракетом. Источник `slot.team` из старых JSON-файлов больше не принимается — генератор СтудЧР переехал на `seed{basket, number}` + верхнеуровневый `teams[]`. Подробности — в [02-domain.md](02-domain.md), [03-sqlite.md](03-sqlite.md), [08-auth.md](08-auth.md), [09-tournaments-and-games.md](09-tournaments-and-games.md).
