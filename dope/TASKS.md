- [ ] Refactor our code without changing functionality. Rn it looks like a crazy bunch of .go files without any structure, please think how it should be better structured and refactor. E.g. we have 3 games now (KSI, EK, OD) but in the future we will have maybe 10–15 games, maybe it makes sense to have a `games` subdirectory / submodule for the games-specific business logic, etc.
- [ ] Add to AGENTS.md instruction to ALWAYS use the design system when creating new pages, extending it if something is lacking but striving to reuse existing parts if possible.
- [ ] Let's try to remove edits contention by mirroring viewer-side event batching (150ms iirc) on the editor-side. Edits done by all editors inside a 150ms window should be batched together to ensure no more than ≈6 writes/sec normally, which should ease database load. A 200 is returned only when all batched edits in the window succeed. A 150ms window should not slow down considerably the editor experience.
- [ ] Right now I think we count viewers globally for all fests, or maybe per fest. Also I think we count all SSE channels, even dead / dormant ones (hours after the game ended I saw ≈70 viewers, it is very unlikely that 70 people still have active tabs opened). Let's partition it per *game*, and count only active channels
- [ ] Last weekend in the first 'battle test' we had an incident where due a sloppy code a mutex failed to unlock and held for an hour, resulting in an outage in production. Two subtask to do for this one:
  - [ ] Make sure that edits and views are separated so that viewers can't stall edits and vice versa. Edits are more important than views so they have priority if there's any contention
  - [ ] Scan the whole codebase and harden it against any possible outage-causing bugs, I want the next fest (to be held 27–28th June) to go silky smooth without any problems
